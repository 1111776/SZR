# 文旅数字人项目讲解

## 这是什么

这是一个面向游客的数字人讲解系统。讲解员名字是阿青。游客自己输入想去的城市或景点，阿青用文字和语音回答，随后接上口型视频。

游客可以打字，也可以点「开始说话」。语音识别在服务器上完成，不依赖浏览器自带的语音识别。

## 现在怎么用

1. 打开讲解页面。
2. 可以先填写想去的景点，也可以直接打招呼。
3. 在输入框打字后点「讲解」，或者点「开始说话」，说完再点一次。
4. 打开页面就能看到阿青。回答会先出现文字，并马上念出来，不用等口型。
5. 口型视频在后台继续生成。好了以后，静止的人像会自动换成她说话的画面。简短讲解大约二十多秒，详细讲解更久。
6. 默认是简短讲解。点「详细讲解」后再提问，会讲得更完整，大约 150 到 220 字。
7. 点「再听一遍」可以重复播放刚才的语音；如果口型已经好了，也会把说话画面再放一遍。

门票、开放时间这类经常变化的信息，回答里不会说死，需要以景区当天公布为准。

## 为什么生成视频慢

慢的部分不是文字回答，而是口型视频。

阿青先把文字变成语音，这一步通常只要几秒。视频要按照语音长度，把讲解员的嘴型一帧一帧算出来，再合成成片。语音越长，帧数越多，等待越久。显卡是 RTX 4090，但这类逐帧人脸渲染不能像普通对话一样瞬间完成。

当前做了这些加速：

- 讲解回答默认控制在大约 35 到 50 字。想听完整的，再切到详细讲解。
- 视频帧率设置为 12，比原来的 25 帧少算很多帧。
- 口型模型和语音识别都在启动时加载一次，后面的提问继续复用，不用每次重新加载。
- 渲染批次从一次 1 帧调整到一次 8 帧。
- 文字和语音先返回，口型视频在后台生成。游客不用等视频才能听到回答，识别也不会被视频堵住。

如果还要更快，只能继续缩短回答、降低分辨率，或者换成更快的口型模型。画质和速度需要取舍。

## 为什么现在主要是嘴在动

当前视频使用 SadTalker。它的作用是根据语音驱动脸上的口型、轻微表情和头部动作，再把脸贴回原始照片。它不是全身动作模型，所以手、衣服和身体不会自然走动。没有人提问时，页面显示阿青的全身照片，不播放一段空视频。

项目里能看到 MuseV 的调用代码，但它的身体动作模型没有随当前环境一起下载。没有这些模型，不能直接生成可靠的全身动作。若以后需要走动、摆手或转身，要单独准备并部署身体驱动模型，再和现在的讲解流程拼接。

## 代码在哪里

- `wenlv_server.py`：讲解服务，负责对话、语音合成、语音识别和视频生成。
- `wenlv_web/index.html`：游客看到的页面。
- `wenlv_config.py`：讲解员名字、讲解风格和回答限制。
- `LLM/DeepSeek.py`：调用 DeepSeek 生成讲解。

DeepSeek 密钥只从环境变量 `DEEPSEEK_API_KEY` 读取，不写进代码，也不提交到仓库。

## 怎么运行

在已经装好 Linly-Talker、PyTorch、FFmpeg、Edge TTS 和 FunASR 的环境里执行：

```bash
export DEEPSEEK_API_KEY=你的密钥
python wenlv_server.py
```

默认地址是 `http://127.0.0.1:6006`。没有显卡时，文字和语音仍可使用，视频生成会提示不可用。
